/** What a client might see as an Article data. */
export type ArticleContent = {
  id: string
  title?: string
  author?: string
  imageAddress?: string
  publicationDate?: string
  excerpt?: string
  body?: string
}
